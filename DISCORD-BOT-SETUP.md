# إعداد بوت Discord على خادم Manga Bubble Cleaner نفسه

## ما يفعله البوت

يعمل البوت داخل **عملية Node.js نفسها** التي تخدم API التبييض على Railway. أعيدت تجربته لتطابق تدفق ZEUS: يبدأ العضو بـ`/clean` أو `!clean`، تظهر لوحة **Components V2** لاختيار الوضع، ثم لوحة المصادر الثلاثة، ثم لوحة حالة حيّة تتغير أثناء القراءة والتبييض والتسليم. لا يكتب البوت الصورة الأصلية أو الناتجة إلى قرص Railway أو إلى MongoDB؛ تبقى البايتات في الذاكرة حتى إرسالها، بينما يحتفظ Discord أو Google Drive بالنتيجة وفق سياسة الوجهة نفسها.

> لا يستعمل البوت إنشاء حسابات مؤقتة، تبديل حسابات، تزوير رؤوس، تجاوز حدود Discord أو أي وسيلة تحايل. إنه بوت واحد رسمي مرتبط بتطبيق Discord واحد ومفتاح واحد محفوظ في متغيرات Railway.

| القيد | السلوك المطبق |
| --- | --- |
| أنواع الصور | PNG وJPG وWebP فقط. |
| حجم الصورة | حتى 20MB، ثم يرفض البوت المرفق قبل تنزيله. |
| ZIP | ملف ZIP واحد حتى 25MB، فيه حتى 5 صور وبحد فك ضغط ذاكرته 100MB. |
| الدفعة | حتى 5 صور مباشرة في العملية الواحدة وبحد إجمالي 30MB لحماية الذاكرة. |
| مصدر التنزيل | نطاقا `cdn.discordapp.com` و`media.discordapp.net` فقط. |
| التوازي | مهمة تبييض واحدة فقط في الوقت نفسه لحماية ذاكرة الخادم. |
| تسليم النتيجة | رد Discord خاص مع ملف PNG؛ لا يصنع الخادم رابط تنزيل أو ملفًا دائمًا. |
| سجل الحالة | MongoDB، إن ضبطته، يخزن معرف المهمة والحالة والخطأ فقط، لا البكسلات. |

## إنشاء تطبيق وبوت رسمي

أنشئ تطبيقًا من [بوابة مطوري Discord](https://discord.com/developers/applications)، ثم افتح تبويب **Bot** وأنشئ مستخدم بوت للتطبيق. تنص وثائق Discord على أن إعداد تطبيق البوت، رمزه، وإعداد OAuth2 توجد في البوابة نفسها.[1]

انسخ **Bot Token** مرة واحدة من تبويب Bot واحفظه في متغير Railway `DISCORD_BOT_TOKEN`. لا ترسله في Discord، ولا تحفظه في GitHub، ولا تضعه في تطبيق Expo. إذا انكشف الرمز فجدده فورًا من البوابة، ثم بدّل متغير Railway.

## دعوة البوت إلى خادم الفريق

من تبويب **OAuth2 → URL Generator** اختر النطاقين `bot` و`applications.commands`. امنح البوت أقل صلاحيات لازمة: **View Channels** و**Send Messages** و**Attach Files** و**Read Message History**. افتح رابط الدعوة الناتج بحساب يملك صلاحية إضافة التطبيقات إلى الخادم، ثم اختر الخادم وقناة اختبار خاصة. يحدد Discord صلاحيات البوت عند تثبيته على الخادم ضمن تدفق OAuth2.[2]

من تبويب **Bot → Privileged Gateway Intents** فعّل **Message Content Intent**. هذا مطلوب لأن مسار المرفقات يطلب من العضو أن يرسل الصور أو ZIP في القناة بعد النقر على الزر؛ يقرأ البوت فقط أول رسالة ذات مرفقات من صاحب الطلب وفي القناة نفسها خلال خمس دقائق.

لا يحتاج البوت إلى معرف خادم محدد. يسجّل الأمر `/clean` كتطبيق **عالمي**، ولذلك يظهر في كل خادم تُدعى إليه. قد يستغرق ظهور أمر عالمي وقتًا حتى يكتمل نشره لدى Discord؛ اختبره أولًا في خادم صغير ثم ادع البوت إلى الخوادم الأخرى.

## متغيرات Railway اللازمة

أضف المتغيرات إلى **الخدمة نفسها** التي تشغّل API التبييض، ثم راجع التغييرات واضغط Deploy. لا تحتاج خدمة Railway ثانية أو Volume أو عامل طابور مستقل.

```dotenv
DISCORD_ENABLED=true
DISCORD_BOT_TOKEN=ضع_رمز_البوت_هنا
```

يبقى `EXTERNAL_OPENAI_API_KEY` ومفتاح MongoDB، إن استُخدم، كما في دليل Railway الأساسي. يبدأ البوت فقط عندما تكون `DISCORD_ENABLED=true` و`DISCORD_BOT_TOKEN` موجودين. عند غياب أحدهما يبقى API الهاتف عاملًا، لكن البوت لا يتصل بـDiscord ولا يسجّل أمرًا عالميًا عن غير قصد.

## استخدام البوت

في قناة اختبار اكتب `/clean` أو `!clean`. تظهر لوحة الاختيار نفسها المستخدمة في ZEUS: زر **سرعة عالية** هو الموصى به ويستخدم الترميم المتوازن، وزر **دقة كاملة** يفعّل الترميم الأعلى. بعد الاختيار تظهر لوحة المصادر الثلاثة ويبدأ البوت انتظار رسالتك في القناة نفسها لخمس دقائق.

| المصدر | ما يرسله العضو | ما يعود إليه |
| --- | --- | --- |
| مرفقات Discord | حتى 5 صور PNG/JPG/WebP في رسالة واحدة بعد ضغط **مرفقات أو ZIP** | صور PNG مرفقة خاصة بأسماء مثل `page-001-clean.png`. |
| ZIP من Discord | ملف ZIP واحد من صور الفصل بعد ضغط **مرفقات أو ZIP** | ملف ZIP واحد يحوي النتائج مرتبة بالأسماء الأساسية نفسها مع لاحقة `-clean`. |
| Google Drive | رابط صورة أو ZIP أو مجلد من خلال نافذة **Google Drive** | مجلد نتائج جديد باسم المصدر مع `-cleaned`، ورابط قارئ لمن يملكه. |

يعرض البوت مرحلة القراءة ثم رقم الصفحة الجاري تنظيفها ثم تجهيز التسليم في **لوحة Components V2 ذات حالة نابضة**، وتُحذف لوحة التقدم عند تسليم النتيجة أو تبقى برسالة خطأ واضحة عند الفشل. سجّل النتيجة بصريًا قبل بدء الترجمة؛ لا يضمن نجاح HTTP أن كشف النص أصاب كل فقاعة.

## إعداد Google Drive

يتطلب هذا المسار حساب خدمة Google مستقلًا؛ ليس مفتاح API عاديًا. فعّل **Google Drive API** في مشروع Google Cloud، وأنشئ حساب خدمة ومفتاح JSON، ثم خزّن محتوى JSON كاملًا في متغير Railway `GOOGLE_SERVICE_ACCOUNT_JSON`. شارك رابط المصدر (الملف أو المجلد) مع البريد الإلكتروني الخاص بحساب الخدمة كـ**Viewer**، أو اجعله قابلاً للقراءة بالرابط. ينشئ البوت مجلد نتائج جديدًا، يرفعه إلى حساب الخدمة، ثم يمنحه `Anyone with the link / Viewer` دون ظهوره في البحث ويعيد رابطه.

> يمنح رابط مجلد النتائج حق القراءة لمن يعرفه. لا ترسل الرابط في قناة عامة إن كانت صفحات الفصل خاصة.

إذا ظهر "الخادم يعالج صفحة أخرى"، انتظر اكتمال المهمة ثم أعد الأمر. إذا ظهر خطأ من مزود كشف النص، لا يعيد البوت صورة أصلية على أنها نتيجة؛ يفشل الطلب صراحةً لحماية سير عملك من نتيجة مضللة.

## اختبار آمن قبل فتحه للفريق

ابدأ بخادم Discord اختبار صغير وبصورة لا تحتوي معلومات حساسة. تحقق من ظهور `/clean` ومن تحميل الناتج، ثم تأكد من Logs Railway أن البوت سجل نفسه ولم يطبع مفتاحًا أو محتوى صورة. بعد نجاح ذلك، ادعُ البوت إلى خادم الفريق الفعلي وحدد الأذونات على القناة المخصصة فقط.

## المراجع

[1]: https://docs.discord.com/developers/bots/overview "Discord Developers — Bots Overview"
[2]: https://docs.discord.com/developers/platform/oauth2-and-permissions "Discord Developers — OAuth2 and Permissions"
