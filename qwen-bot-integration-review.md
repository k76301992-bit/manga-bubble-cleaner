# مراجعة تقنية لسكربت استخراج النص المرفق

## الخلاصة التنفيذية

الملف هو **بوت Discord لاستخراج النصوص** عبر واجهة خدمة Qwen غير موثقة ظاهرًا، وليس محركًا جاهزًا لكشف مواضع النص داخل الصورة. يستطيع أن يكون مرجعًا لفهم تسلسل رفع الصورة ثم إنشاء محادثة ثم قراءة الرد المتدفق، لكنه لا يصلح للنسخ الحرفي داخل تطبيق تنظيف الفقاعات. تغيير البرومبت وحده لا يكفي، لأن التطبيق يحتاج إحداثيات أو قناعًا، بينما السكربت يطلب من النموذج نصًا خامًا فقط.

## ما الذي يفعله السكربت بالفعل

| الطبقة | السلوك في السكربت | الفائدة المحتملة للتطبيق |
|---|---|---|
| واجهة Discord | أمر `/extract`، وضع دقة/سرعة، استلام حتى خمس صور أو ZIP أو رابط Drive | غير مطلوب لتطبيق Expo الحالي. |
| جلب الصور | مرفقات Discord، رابط مباشر، Drive، أو ZIP مع ترتيب طبيعي للأسماء | يمكن اقتباس فكرة **الترتيب المتسلسل** فقط؛ التطبيق لديه استيراد من الجهاز والرابط أصلًا. |
| رفع الصورة | يطلب تصريح رفع مؤقتًا ثم يرفع الصورة إلى تخزين خدمة Qwen على مراحل | يلزم فقط إن استخدم موفر خارجي معتمد ويشترط رفعًا خاصًا به. |
| الاستدلال | ينشئ محادثة ويرسل صورة وبرومبت استخراج نص، ثم يقرأ SSE حتى اكتمال الرد | هذه هي الفكرة المفيدة، لكن يلزم تغيير عقدة الخرج إلى JSON منظم. |
| الناتج | ملف TXT يضم النصوص حسب ترتيب الصور | غير كافٍ للتبييض؛ لا يضم صناديق أو مضلعات أو أقنعة. |

## تدفق Qwen داخل الملف

1. يحصل السكربت على رمز وصول للحساب ثم يبني ترويسات جلسة تتضمن رمز Bearer وCookie ومعرّف جهاز.
2. يطلب بيانات رفع قصيرة العمر من مسار ملفات الخدمة.
3. ينفذ رفعًا متعدد المراحل إلى تخزين OSS باستخدام بيانات STS التي عادت من الخدمة.
4. ينشئ محادثة جديدة، ثم يرسل `chat/completions` مع الصورة وبرومبت OCR ونموذج رؤية محدد.
5. يقرأ الاستجابة المتدفقة `SSE` ويجمع فقط الرسائل التي تحمل مرحلة الإجابة.
6. عند «RateLimited» يحاول تبديل الحساب ثم يعيد الطلب.

> **استنتاج مهم:** هذا التسلسل يصف استخراج **المحتوى النصي**. لا يوجد فيه تحليل كود يعيد `x/y/width/height`، ولا تحويل من النص إلى موضعه داخل الصورة.

## لماذا لا يكفي تعديل البرومبت فقط؟

يمكن لبرومبت جديد أن يطلب من نموذج رؤية إعادة JSON مثل `boxes` أو `polygons`، لكن نجاح الدمج يتطلب أربع تغييرات إضافية: التحقق الصارم من JSON، تحويل الإحداثيات إلى أبعاد الصفحة الأصلية، حصر الكشف داخل فقاعات الحوار، وإرسال المناطق الدقيقة إلى محرك الترميم الحالي. إذا أعاد النموذج نصًا حرًا أو إحداثيات غير صالحة، فلا يجوز تشغيل التبييض عليها آليًا.

العقدة المناسبة للتطبيق هي:

```ts
type DetectedBubbleText = {
  box: { x: number; y: number; width: number; height: number };
  polygon?: Array<{ x: number; y: number }>;
  confidence: number;
  kind: "dialogue" | "caption";
};
```

ثم يمرَّر `box` أو `polygon` إلى الترميم المحلي. القناع المضلع أدق من المستطيل عند الحروف المائلة أو الظلال، وهو ما تحتاجه الفقاعات الملونة الحساسة.

## عناصر لا ينبغي نسخها

الملف يتضمن نمطًا لإنشاء حسابات مؤقتة عبر بريد مؤقت وتبديلها تلقائيًا عند بلوغ الحد، كما يتضمن محاكاة ترويسات تطبيق هاتف. لن أدمج هذا النمط أو أعيد إنتاجه؛ فهو غير موثوق ويمكن أن يخالف شروط الموفر. يجب أن يعتمد التكامل على حساب أو واجهة API مصرح بها وتدفق OAuth أو مفتاح خدمة رسمي محفوظ في الخادم فقط.

كذلك يحتوي الملف المرفق على بيانات اعتماد حساسة مضمنة، منها رمز بوت ومفتاح وصول لخدمة خارجية. لا ينبغي نقلها إلى التطبيق أو مشاركتها. يجب تدويرها فورًا من لوحات مزوديها وحذفها من الملف/سجل Git، ثم استخدام متغيرات بيئة سرية في الخادم.

## ملاحظات جودة وأمان في السكربت

| الملاحظة | الأثر |
|---|---|
| لا يعيد إحداثيات أو قناعًا | لا يمكن توصيله مباشرة بالترميم. |
| لا يتحقق فعليًا من حد حجم الصورة رغم تعريف حد ثابت | قد يقبل مدخلات كبيرة وتستهلك الذاكرة. |
| روابط مباشرة وتنزيل ZIP بلا ضوابط كافية | يحتاج حماية SSRF وحدود الحجم وفحص الأنواع وتقييد محتوى ZIP. |
| الحسابات والرموز وكلمات المرور تحفظ في JSON عادي | غير مناسب لخدمة فريق أو إنتاج. |
| إعادة المحاولة تعتمد على استدعاء ذاتي عند الحد | قد تتكرر بلا ضبط واضح أو تتلف الطابور. |
| يستخدم `os` من دون استيراده في بداية الملف | يبدو أنه سيفشل عند التشغيل ما لم يكن هناك استيراد محذوف. |

## خطة الدمج الصحيحة

1. اختيار **موفر رؤية مصرح به** يمكنه إعادة JSON منظّم لمواضع نص الفقاعات، أو استخدام واجهة Qwen الرسمية إذا كانت متاحة للحساب.
2. بناء موفر خلفي مستقل باسم `TextDetectionProvider` بدل نسخ كود Discord أو إدارة الحسابات.
3. حفظ بيانات الاعتماد كأسرار خادم، وعدم تمريرها أبدًا إلى تطبيق Expo أو تخزينها محليًا.
4. إرسال الصفحة على مقاطع متسلسلة كما يفعل التطبيق، ثم تطبيع الإحداثيات ودمج المناطق المتداخلة.
5. عرض القناع للمراجع قبل الترميم، ووضع المناطق ضعيفة الثقة في حالة **تحتاج مراجعة** بدل تخريب الرسم.

بهذه الطريقة يمكن تبديل موفر الكشف لاحقًا من دون تغيير محرك التبييض المحلي أو واجهات المشروع والطابور.
